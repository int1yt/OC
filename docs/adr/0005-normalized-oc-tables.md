# 数据模型：OC 子结构用独立关系表

OC 的子结构（外貌条目、能力值、核心价值观、优缺点、口头禅、扩展字段）采用独立关系表（以 ocId 关联），而非内嵌 JSON 列；能力维度模板为作品级独立表，OC 的能力值表记录其实际维度名与 0-10 分值。

理由：这些子结构需参与筛选、关联与 JSON 导出的一致性，归一化后结构统一、便于按维度/标签查询；OC 详情整体加载时在同一事务内取回所有子表，读成本可控。权衡：表数量增多，但本地 SQLite 下代价低。
